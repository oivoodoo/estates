- content_for :buttons do
  = link_to "Add new Project", new_admin_project_path, class: "btn btn-block btn-primary btn btn-large"

%table.table.table-hover
  %thead
    %tr
      %th Project name
      %th Price, $
      %th Owner
      %th Actions

  %tbody
    = render partial: 'project', collection: collection

= link_to "Back to Admin", admin_path


