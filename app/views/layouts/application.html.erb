<!DOCTYPE html>
<html dir="ltr" lang="en">
<head>
  <title>Flock Captial</title>
  <meta charset="utf-8">
  <meta name="description" content="html meta description" />
  <meta name="keywords" content="html, meta, keywords" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />

  <link rel="canonical" href="http://flock.ips/" />

  <link rel="Shortcut Icon" type="image/svg" href="<%= image_path('g/flockinvest-logo-symbol.svg') %>">

  <%= render 'layouts/js' %>

  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= stylesheet_link_tag "application" %>
  <%= csrf_meta_tags %>

  <!-- sensor=false tells google images we aren't trying to sense the user's location -->
  <script src="http://maps.google.com/maps/api/js?sensor=false"></script>

  <!--[if lte IE 8]> <%= stylesheet_link_tag "ie8" %><![endif]-->
</head>
<body onload="" class="<%= layout_class %>" ng-controller='ApplicationController'>
  <%= gflash %>

  <div id="wrap">

    <header id="masthead" class='<%= header_class %>'>
      <div>
        <div id="logo">
          <div>
            <h1 itemprop="logo">
              <%= link_to root_path do %>
                <%= render 'layouts/top_logo' %>
              <% end %>
            </h1>
          </div>
          <turbo-loading></turbo-loading>
        </div>
        <nav id='nav'>
          <div>
            <%= render 'layouts/header' %>
          </div>
        </nav>
        <a id="spread"><i></i></a>
      </div>
    </header>

    <div id="main">
      <%= yield %>

      <div id="rise">
        <div>
          <canvas id="riseCanvas" width="1440" height="119"></canvas>
        </div>
      </div>
    </div>

    <footer id='footer'>
      <%= render "layouts/footer" %>
    </footer>
  </div><!-- #wrap -->

  <div id="nag" style="visibility: visible; opacity: 1;">
    <div>
      <span><a class="button flat thickest sign-in fancybox.ajax" href="<%= new_user_registration_path %>">Sign up</a> or <a class="button flat thickest sign-in fancybox.ajax" href="<%= new_user_session_path %>">sign in</a></span> <span>to follow projects</span>
    </div>
    <div class="close"></div>
  </div>

</body>
</html>

