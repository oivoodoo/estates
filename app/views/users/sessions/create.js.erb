<%= add_gritter(flash.now[:success], :image => :success) %>
$('#nav').find('div:first-child').html('<%= escape_javascript(render(partial: 'layouts/header')) %>');
$('#js-settings').replaceWith('<%= escape_javascript(render(partial: 'layouts/js')) %>');
$(document).trigger('user:signed_in')
$.fancybox.close()
$('.sign-in button').each(function(i, button) {
  // extract buttons in case if wrap action that need to have signed in user.
  var button = $(button);
  var signupButton = $(button).parent();
  signupButton.next('button').show();
  signupButton.remove();
});
$('#slideshow .action').remove();

